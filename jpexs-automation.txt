JPEXS / GFX automation notes

Current state:
- `build_all.ps1` only rebuilds/repackages the extracted GFXF payloads that already exist under `src\hud.swf\gfx\...` and `src\menu.swf\gfx\...`.
- It does not import `.as` source changes from `src\hud.swf\scripts\...` or `src\menu.swf\scripts\...` into the underlying `.GFX` binaries.
- That manual import step is what is currently being done in JPEXS before running `build_all.ps1`.

Relevant extracted GFX targets:
- Gameplay HUD GFX:
  `src\hud.swf\gfx\GFXF\chunk0.rpkg\01262EED9D687969.GFXF\01262EED9D687969.GFXF.GFX`
- Menu GFX:
  `src\menu.swf\gfx\GFXF\chunk0.rpkg\01A837AF55107DEC.GFXF\01A837AF55107DEC.GFXF.GFX`

Intended future automation:
- Create a separate script, likely `sync_sources_to_gfx.ps1`.
- Use JPEXS / FFDec CLI to import or replace ActionScript classes into the two `.GFX` files above.
- Map:
  - `src\hud.swf\scripts\...` -> gameplay `.GFX`
  - `src\menu.swf\scripts\...` -> menu `.GFX`
- After source sync completes, run `.\build_all.ps1` as the existing package rebuild step.

Environment notes from investigation:
- `java` is installed on this machine.
- `ffdec` / JPEXS CLI was not found on PATH during the check.
- Next time this is resumed, either:
  - provide the local JPEXS install path, or
  - locate it on disk and inspect supported CLI commands.

Important caveat:
- Need to confirm the exact JPEXS CLI command for batch ActionScript replacement against these `.GFX` files before scripting.
- The repo search did not show an existing source-to-GFX import script.
